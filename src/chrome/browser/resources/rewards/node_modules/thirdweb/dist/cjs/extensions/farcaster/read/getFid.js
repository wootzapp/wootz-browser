"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getFid = getFid;
const withCache_js_1 = require("../../../utils/promise/withCache.js");
const idOf_js_1 = require("../__generated__/IIdRegistry/read/idOf.js");
const getIdRegistry_js_1 = require("../contracts/getIdRegistry.js");
/**
 * Retrieves the current fid for an account.
 * @param options - Parameters to pass to the function.
 * @returns A promise that resolves to the account's fid, if one exists, otherwise 0.
 * @extension FARCASTER
 * @example
 * ```ts
 * import { getFid } from "thirdweb/extensions/farcaster";
 *
 * const price = await getFid({
 *  client,
 * 	address,
 * });
 * ```
 */
async function getFid(options) {
    const fetch = () => {
        const contract = (0, getIdRegistry_js_1.getIdRegistry)({
            client: options.client,
            chain: options.chain,
        });
        return (0, idOf_js_1.idOf)({ contract, owner: options.address });
    };
    return (0, withCache_js_1.withCache)(fetch, {
        cacheKey: `${options.address}:getFid`,
        cacheTime: options.disableCache ? 0 : 60 * 60 * 1000, // 60 minutes
    });
}
//# sourceMappingURL=getFid.js.map