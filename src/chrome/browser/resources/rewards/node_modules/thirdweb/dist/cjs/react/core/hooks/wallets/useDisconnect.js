"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useDisconnect = useDisconnect;
const connection_manager_js_1 = require("../../providers/connection-manager.js");
/**
 * Disconnect from given account
 * @example
 * ```jsx
 * import { useDisconnect, useActiveWallet } from "thirdweb/react";
 *
 * function Example() {
 *   const { disconnect } = useDisconnect();
 *   const wallet = useActiveWallet();
 *
 *   return (
 *     <button onClick={() => disconnect(wallet)}>
 *       Disconnect
 *     </button>
 *   );
 * }
 * ```
 * @walletConnection
 * @returns An object with a function to disconnect an account
 */
function useDisconnect() {
    const manager = (0, connection_manager_js_1.useConnectionManagerCtx)("useDisconnect");
    const disconnect = manager.disconnectWallet;
    return { disconnect };
}
//# sourceMappingURL=useDisconnect.js.map