"use client";
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { keyframes } from "@emotion/react";
import styled from "@emotion/styled";
import * as RadixTooltip from "@radix-ui/react-tooltip";
import { useCustomTheme } from "../../../core/design-system/CustomThemeProvider.js";
import { fontSize, radius, shadow, spacing, } from "../../../core/design-system/index.js";
/**
 * @internal
 */
export const ToolTip = (props) => {
    return (_jsx(RadixTooltip.Provider, { delayDuration: 200, children: _jsxs(RadixTooltip.Root, { children: [_jsx(RadixTooltip.Trigger, { asChild: true, children: props.children }), _jsx(RadixTooltip.Portal, { children: _jsxs(TooltipContent, { sideOffset: props.sideOffset || 6, align: props.align || "center", side: props.side || "top", children: [props.tip, _jsx(TooltipArrow, {})] }) })] }) }));
};
const slideUpAndFade = keyframes `
from {
  opacity: 0;
  transform: translateY(2px);
}
to {
  opacity: 1;
  transform: translateY(0);
}
`;
const TooltipContent = /* @__PURE__ */ (() => styled(RadixTooltip.Content)((_) => {
    const theme = useCustomTheme();
    return {
        background: theme.colors.tooltipBg,
        color: theme.colors.tooltipText,
        borderRadius: radius.sm,
        padding: `${spacing.xs} ${spacing.sm}`,
        fontSize: fontSize.sm,
        boxShadow: shadow.sm,
        userSelect: "none",
        willChange: "transform, opacity",
        animation: `${slideUpAndFade} 200ms cubic-bezier(0.16, 1, 0.3, 1)`,
        zIndex: 999999999999999,
        maxWidth: "300px",
        lineHeight: 1.5,
    };
}))();
const TooltipArrow = /* @__PURE__ */ (() => styled(RadixTooltip.Arrow)(() => {
    const theme = useCustomTheme();
    return {
        fill: theme.colors.tooltipBg,
    };
}))();
//# sourceMappingURL=Tooltip.js.map