"use client";
import { jsx as _jsx } from "react/jsx-runtime";
import { spacing } from "../../../core/design-system/index.js";
import { useClipboard } from "../hooks/useCopyClipboard.js";
import { IconButton } from "./buttons.js";
import { Text } from "./text.js";
/**
 * @internal
 */
export const CopyAddress = (props) => {
    const { hasCopied, onCopy } = useClipboard(props.addressOrEns);
    return (_jsx(IconButton, { onKeyDown: (e) => {
            if (e.key === "Enter" || e.key === " ") {
                onCopy();
            }
        }, onClick: onCopy, style: { padding: `${spacing["4xs"]} ${spacing.xxs}` }, children: _jsx(Text, { color: hasCopied ? "primaryText" : "secondaryText", weight: 400, size: "sm", children: hasCopied ? "Copied address!" : props.addressOrEns }) }));
};
//# sourceMappingURL=CopyAddress.js.map