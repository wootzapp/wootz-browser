import { prepareEvent } from "../../../../../event/prepare-event.js";
/**
 * Creates an event object for the StakeUnlocked event.
 * @param filters - Optional filters to apply to the event.
 * @returns The prepared event object.
 * @extension ERC4337
 * @example
 * ```ts
 * import { getContractEvents } from "thirdweb";
 * import { stakeUnlockedEvent } from "thirdweb/extensions/erc4337";
 *
 * const events = await getContractEvents({
 * contract,
 * events: [
 *  stakeUnlockedEvent({
 *  account: ...,
 * })
 * ],
 * });
 * ```
 */
export function stakeUnlockedEvent(filters = {}) {
    return prepareEvent({
        signature: "event StakeUnlocked(address indexed account, uint256 withdrawTime)",
        filters,
    });
}
//# sourceMappingURL=StakeUnlocked.js.map