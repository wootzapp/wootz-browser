import { jsx as _jsx } from "react/jsx-runtime";
import { useState } from "react";
import { FiatSteps } from "./FiatSteps.js";
import { PostOnRampSwap } from "./PostOnRampSwap.js";
// Note: It is necessary to lock in the fiat-status in state and only pass that to <PostOnRampSwap /> so it does not suddenly change during the swap process.
/**
 * - Show 2 steps UI with step 2 highlighted, on continue button click:
 * - Show swap flow
 */
export function PostOnRampSwapFlow(props) {
    const [statusForSwap, setStatusForSwap] = useState();
    // step 2 flow
    if (statusForSwap) {
        return (_jsx(PostOnRampSwap, { title: props.title, buyWithFiatStatus: statusForSwap, client: props.client, onDone: props.onDone, transactionMode: props.transactionMode, isEmbed: props.isEmbed, payer: props.payer, onSuccess: props.onSuccess }));
    }
    // show step 1 and step 2 details
    return (_jsx(FiatSteps, { title: props.title, client: props.client, onBack: props.onBack, partialQuote: props.quote, step: 2, onContinue: () => {
            props.onSwapFlowStarted();
            setStatusForSwap(props.status);
        }, status: props.status }));
}
//# sourceMappingURL=PostOnRampSwapFlow.js.map