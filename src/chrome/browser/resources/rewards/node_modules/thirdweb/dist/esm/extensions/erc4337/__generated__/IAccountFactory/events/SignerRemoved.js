import { prepareEvent } from "../../../../../event/prepare-event.js";
/**
 * Creates an event object for the SignerRemoved event.
 * @param filters - Optional filters to apply to the event.
 * @returns The prepared event object.
 * @extension ERC4337
 * @example
 * ```ts
 * import { getContractEvents } from "thirdweb";
 * import { signerRemovedEvent } from "thirdweb/extensions/erc4337";
 *
 * const events = await getContractEvents({
 * contract,
 * events: [
 *  signerRemovedEvent({
 *  account: ...,
 *  signer: ...,
 * })
 * ],
 * });
 * ```
 */
export function signerRemovedEvent(filters = {}) {
    return prepareEvent({
        signature: "event SignerRemoved(address indexed account, address indexed signer)",
        filters,
    });
}
//# sourceMappingURL=SignerRemoved.js.map