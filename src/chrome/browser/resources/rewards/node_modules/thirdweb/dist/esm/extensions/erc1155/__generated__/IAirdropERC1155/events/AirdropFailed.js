import { prepareEvent } from "../../../../../event/prepare-event.js";
/**
 * Creates an event object for the AirdropFailed event.
 * @param filters - Optional filters to apply to the event.
 * @returns The prepared event object.
 * @extension ERC1155
 * @example
 * ```ts
 * import { getContractEvents } from "thirdweb";
 * import { airdropFailedEvent } from "thirdweb/extensions/erc1155";
 *
 * const events = await getContractEvents({
 * contract,
 * events: [
 *  airdropFailedEvent({
 *  tokenAddress: ...,
 *  tokenOwner: ...,
 *  recipient: ...,
 * })
 * ],
 * });
 * ```
 */
export function airdropFailedEvent(filters = {}) {
    return prepareEvent({
        signature: "event AirdropFailed(address indexed tokenAddress, address indexed tokenOwner, address indexed recipient, uint256 tokenId, uint256 amount)",
        filters,
    });
}
//# sourceMappingURL=AirdropFailed.js.map