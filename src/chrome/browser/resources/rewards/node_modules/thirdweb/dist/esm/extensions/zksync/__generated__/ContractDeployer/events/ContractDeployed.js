import { prepareEvent } from "../../../../../event/prepare-event.js";
/**
 * Creates an event object for the ContractDeployed event.
 * @param filters - Optional filters to apply to the event.
 * @returns The prepared event object.
 * @extension ZKSYNC
 * @example
 * ```ts
 * import { getContractEvents } from "thirdweb";
 * import { contractDeployedEvent } from "thirdweb/extensions/zksync";
 *
 * const events = await getContractEvents({
 * contract,
 * events: [
 *  contractDeployedEvent({
 *  deployerAddress: ...,
 *  bytecodeHash: ...,
 *  contractAddress: ...,
 * })
 * ],
 * });
 * ```
 */
export function contractDeployedEvent(filters = {}) {
    return prepareEvent({
        signature: "event ContractDeployed(address indexed deployerAddress, bytes32 indexed bytecodeHash, address indexed contractAddress)",
        filters,
    });
}
//# sourceMappingURL=ContractDeployed.js.map