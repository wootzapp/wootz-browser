import { getClientFetch } from "../../../../../utils/fetch.js";
import { randomBytesHex } from "../../../../../utils/random.js";
import { ROUTE_EMBEDDED_WALLET_DETAILS, ROUTE_IS_VALID_USER_MANAGED_OTP, ROUTE_STORE_USER_SHARES, ROUTE_USER_MANAGED_OTP, ROUTE_VALIDATE_USER_MANAGED_OTP, ROUTE_VERIFY_COGNITO_OTP, ROUTE_VERIFY_THIRDWEB_CLIENT_ID, THIRDWEB_SESSION_NONCE_HEADER, } from "../constants.js";
import { createErrorMessage } from "../errors.js";
import { getAuthTokenClient } from "../storage/local.js";
const EMBEDDED_WALLET_TOKEN_HEADER = "embedded-wallet-token";
const PAPER_CLIENT_ID_HEADER = "x-thirdweb-client-id";
let sessionNonce = undefined;
export function getSessionHeaders() {
    if (!sessionNonce) {
        sessionNonce = randomBytesHex(16);
    }
    return {
        "Content-Type": "application/json",
        [THIRDWEB_SESSION_NONCE_HEADER]: sessionNonce,
    };
}
export const verifyClientId = async (client) => {
    const resp = await getClientFetch(client)(ROUTE_VERIFY_THIRDWEB_CLIENT_ID, {
        method: "POST",
        headers: {
            ...getSessionHeaders(),
        },
        body: JSON.stringify({ clientId: client.clientId, parentDomain: "" }),
    });
    if (!resp.ok) {
        const error = await resp.json();
        throw new Error(`Something went wrong generating auth token from user cognito email otp. ${error.message}`);
    }
    return {
        success: true,
    };
};
export const authFetchEmbeddedWalletUser = async (client, url, props, retries = 3) => {
    const authTokenClient = await getAuthTokenClient(client.clientId);
    const params = { ...props };
    params.headers = params?.headers
        ? {
            ...params.headers,
            Authorization: `Bearer ${EMBEDDED_WALLET_TOKEN_HEADER}:${authTokenClient || ""}`,
            [PAPER_CLIENT_ID_HEADER]: client.clientId,
            ...getSessionHeaders(),
        }
        : {
            Authorization: `Bearer ${EMBEDDED_WALLET_TOKEN_HEADER}:${authTokenClient || ""}`,
            [PAPER_CLIENT_ID_HEADER]: client.clientId,
            ...getSessionHeaders(),
        };
    try {
        return await getClientFetch(client)(url, params);
    }
    catch (e) {
        if (retries > 0) {
            await new Promise((resolve) => setTimeout(resolve, 500));
            return await authFetchEmbeddedWalletUser(client, url, params, retries - 1);
        }
        else {
            throw e;
        }
    }
};
export async function fetchUserDetails(args) {
    const url = new URL(ROUTE_EMBEDDED_WALLET_DETAILS);
    if (args) {
        // TODO (inapp) remove this, unused in the backend but still required
        url.searchParams.append("email", args.email ?? "none");
        url.searchParams.append("clientId", args.client.clientId);
    }
    const resp = await authFetchEmbeddedWalletUser(args.client, url.href, {
        method: "GET",
    });
    if (!resp.ok) {
        const error = await resp.json();
        throw new Error(`Something went wrong determining wallet type. ${error.message}`);
    }
    const result = (await resp.json());
    return result;
}
export async function generateAuthTokenFromCognitoEmailOtp(session, clientId) {
    const resp = await fetch(ROUTE_VERIFY_COGNITO_OTP, {
        method: "POST",
        headers: {
            ...getSessionHeaders(),
        },
        body: JSON.stringify({
            access_token: session.getAccessToken().getJwtToken(),
            refresh_token: session.getRefreshToken().getToken(),
            id_token: session.getIdToken().getJwtToken(),
            developerClientId: clientId,
            otpMethod: "email",
        }),
    });
    if (!resp.ok) {
        const error = await resp.json();
        throw new Error(`Something went wrong generating auth token from user cognito email otp. ${error.message}`);
    }
    const respJ = await resp.json();
    return respJ;
}
export async function sendUserManagedEmailOtp(email, clientId) {
    const resp = await fetch(ROUTE_USER_MANAGED_OTP, {
        method: "POST",
        headers: {
            ...getSessionHeaders(),
        },
        body: JSON.stringify({
            email,
            clientId,
        }),
    });
    if (!resp.ok) {
        const error = await resp.json();
        throw new Error(`Something went wrong generating auth token from user cognito email otp. ${error.message}`);
    }
    const respJ = await resp.json();
    return respJ;
}
export async function validateUserManagedEmailOtp(options) {
    const resp = await fetch(ROUTE_VALIDATE_USER_MANAGED_OTP, {
        method: "POST",
        headers: {
            ...getSessionHeaders(),
        },
        body: JSON.stringify({
            email: options.email,
            otp: options.otp,
            clientId: options.clientId,
        }),
    });
    if (!resp.ok) {
        const error = await resp.json();
        throw new Error(`Something went wrong generating auth token from user cognito email otp. ${error.message}`);
    }
    const respJ = await resp.json();
    return respJ;
}
export async function isValidUserManagedEmailOtp(options) {
    const resp = await fetch(ROUTE_IS_VALID_USER_MANAGED_OTP, {
        method: "POST",
        headers: {
            ...getSessionHeaders(),
        },
        body: JSON.stringify({
            email: options.email,
            otp: options.otp,
            clientId: options.clientId,
        }),
    });
    if (!resp.ok) {
        const error = await resp.json();
        throw new Error(`Something went wrong generating auth token from user cognito email otp. ${error.message}`);
    }
    const respJ = await resp.json();
    return respJ;
}
export async function storeUserShares({ client, walletAddress, maybeEncryptedRecoveryShares, authShare, }) {
    const resp = await authFetchEmbeddedWalletUser(client, ROUTE_STORE_USER_SHARES, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
            walletAddress,
            maybeEncryptedRecoveryShares,
            authShare,
        }),
    });
    if (!resp.ok) {
        const error = await resp.json();
        throw new Error(`Something went wrong storing user wallet shares: ${JSON.stringify(error.message, null, 2)}`);
    }
}
export async function getUserShares(client, getShareUrl) {
    const resp = await authFetchEmbeddedWalletUser(client, getShareUrl.href, {
        method: "GET",
    });
    if (!resp.ok) {
        const error = await resp.json();
        throw new Error(`Something went wrong getting user's wallet: ${JSON.stringify(error.message, null, 2)} `);
    }
    const respJ = await resp.json();
    try {
        return respJ;
    }
    catch (e) {
        throw new Error(createErrorMessage("Malformed response from the ews user wallet API", e));
    }
}
export async function deleteAccount(client) {
    const url = new URL(ROUTE_EMBEDDED_WALLET_DETAILS);
    const resp = await authFetchEmbeddedWalletUser(client, url.href, {
        method: "DELETE",
    });
    if (!resp.ok) {
        const error = await resp.json();
        throw new Error(`Something went wrong deleting the active account: ${error.message}`);
    }
    return await resp.json();
}
//# sourceMappingURL=fetchers.js.map