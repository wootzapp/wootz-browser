"use client";
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { keyframes } from "@emotion/react";
import { OTPInput as InputOTP } from "input-otp";
import { useCustomTheme } from "../../../core/design-system/CustomThemeProvider.js";
import { fontSize, radius, spacing, } from "../../../core/design-system/index.js";
import { StyledDiv } from "../design-system/elements.js";
import { Container } from "./basic.js";
/**
 * @internal
 */
export function OTPInput(props) {
    return (_jsx(OTPInputContainer, { children: _jsx(InputOTP, { onComplete: () => {
                props.onEnter();
            }, maxLength: 6, value: props.value, render: ({ slots }) => (_jsx(Container, { flex: "row", gap: "xs", center: "both", children: slots.map((slot, idx) => (
                // biome-ignore lint/suspicious/noArrayIndexKey: index is the only valid key here
                _jsx(Slot, { ...slot, isInvalid: props.isInvalid }, idx))) })), onChange: (newValue) => {
                props.setValue(newValue);
            } }) }));
}
const OTPInputContainer = /* @__PURE__ */ StyledDiv({
    "& input": {
        maxWidth: "100%",
    },
});
function Slot(props) {
    return (_jsxs(OTPInputBox, { "data-active": props.isActive, "data-error": props.isInvalid, children: [props.char !== null && _jsx("div", { children: props.char }), props.hasFakeCaret && _jsx(FakeCaret, {})] }));
}
const caretBlink = keyframes `
  0%, 100% {
    opacity: 0;
  },
  50% {
    opacity: 1;
  }
  `;
const FakeCaret = StyledDiv((_) => {
    const theme = useCustomTheme();
    return {
        position: "absolute",
        pointerEvents: "none",
        inset: 0,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        animation: `${caretBlink} 1s infinite`,
        "&::before": {
            content: "''",
            display: "block",
            width: "1.5px",
            height: "1em",
            backgroundColor: theme.colors.primaryText,
        },
    };
});
const OTPInputBox = /* @__PURE__ */ StyledDiv((_) => {
    const theme = useCustomTheme();
    return {
        position: "relative",
        width: "40px",
        height: "40px",
        textAlign: "center",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: fontSize.md,
        padding: spacing.xs,
        boxSizing: "border-box",
        transition: "color 200ms ease",
        border: `2px solid ${theme.colors.borderColor}`,
        "&[data-active='true']": {
            borderColor: theme.colors.accentText,
        },
        color: theme.colors.primaryText,
        borderRadius: radius.lg,
        "&[data-error='true']": {
            borderColor: theme.colors.danger,
        },
    };
});
//# sourceMappingURL=OTPInput.js.map