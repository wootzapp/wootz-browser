import { prepareEvent } from "../../../../../event/prepare-event.js";
/**
 * Creates an event object for the UserOperationEvent event.
 * @param filters - Optional filters to apply to the event.
 * @returns The prepared event object.
 * @extension ERC4337
 * @example
 * ```ts
 * import { getContractEvents } from "thirdweb";
 * import { userOperationEventEvent } from "thirdweb/extensions/erc4337";
 *
 * const events = await getContractEvents({
 * contract,
 * events: [
 *  userOperationEventEvent({
 *  userOpHash: ...,
 *  sender: ...,
 *  paymaster: ...,
 * })
 * ],
 * });
 * ```
 */
export function userOperationEventEvent(filters = {}) {
    return prepareEvent({
        signature: "event UserOperationEvent(bytes32 indexed userOpHash, address indexed sender, address indexed paymaster, uint256 nonce, bool success, uint256 actualGasCost, uint256 actualGasUsed)",
        filters,
    });
}
//# sourceMappingURL=UserOperationEvent.js.map