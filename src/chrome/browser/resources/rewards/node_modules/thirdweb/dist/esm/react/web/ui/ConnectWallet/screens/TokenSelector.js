import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import styled from "@emotion/styled";
import { ChevronDownIcon, CrossCircledIcon } from "@radix-ui/react-icons";
import { useQuery } from "@tanstack/react-query";
import { useState } from "react";
import { getTokenBalance } from "../../../../../wallets/utils/getTokenBalance.js";
import { useCustomTheme } from "../../../../core/design-system/CustomThemeProvider.js";
import { fontSize, iconSize, spacing, } from "../../../../core/design-system/index.js";
import { useChainIconUrl, useChainName, } from "../../../../core/hooks/others/useChainQuery.js";
import { useTokenInfo } from "../../../../core/hooks/others/useTokenInfo.js";
import { useActiveAccount } from "../../../../core/hooks/wallets/useActiveAccount.js";
import { ChainIcon } from "../../components/ChainIcon.js";
import { Skeleton } from "../../components/Skeleton.js";
import { Spacer } from "../../components/Spacer.js";
import { Spinner } from "../../components/Spinner.js";
import { TokenIcon } from "../../components/TokenIcon.js";
import { Container, Line, ModalHeader } from "../../components/basic.js";
import { Button } from "../../components/buttons.js";
import { Input } from "../../components/formElements.js";
import { Text } from "../../components/text.js";
import { ChainButton, NetworkSelectorContent } from "../NetworkSelector.js";
import { formatTokenBalance } from "./formatTokenBalance.js";
import { NATIVE_TOKEN, isNativeToken, } from "./nativeToken.js";
// Note: TokenSelector can be used when wallet may or may not be connected
/**
 *
 * @internal
 */
export function TokenSelector(props) {
    const [screen, setScreen] = useState("base");
    const [input, setInput] = useState("");
    const chain = props.chain;
    const chainNameQuery = useChainName(chain);
    const chainIconQuery = useChainIconUrl(chain);
    // if input is undefined, it loads the native token
    // otherwise it loads the token with given address
    const tokenQuery = useTokenInfo({
        chain: chain,
        tokenAddress: input,
        client: props.client,
    });
    const locale = props.connectLocale.sendFundsScreen;
    let tokenList = props.tokenList;
    if (tokenQuery.data && input) {
        tokenList = [
            {
                ...tokenQuery.data,
                address: input,
            },
            ...tokenList,
        ];
    }
    const filteredList = input
        ? tokenList.filter((t) => {
            const inputStr = input.toLowerCase();
            return (t.name.toLowerCase().includes(inputStr) ||
                t.symbol.toLowerCase().includes(inputStr) ||
                t.address.includes(input));
        })
        : tokenList;
    const { chainSelection } = props;
    if (screen === "select-chain" && chainSelection) {
        return (_jsx(NetworkSelectorContent, { client: props.client, connectLocale: props.connectLocale, showTabs: false, onBack: () => setScreen("base"), 
            // pass swap supported chains
            chains: chainSelection.chains, closeModal: () => setScreen("base"), networkSelector: {
                renderChain(renderChainProps) {
                    return (_jsx(ChainButton, { chain: renderChainProps.chain, confirming: false, switchingFailed: false, onClick: () => {
                            chainSelection.select(renderChainProps.chain);
                            setScreen("base");
                        }, client: props.client, connectLocale: props.connectLocale }));
                },
            } }));
    }
    return (_jsxs(Container, { animate: "fadein", style: {
            minHeight: "300px",
        }, children: [_jsx(Container, { p: "lg", children: _jsx(ModalHeader, { onBack: props.onBack, title: props.modalTitle || locale.selectTokenTitle }) }), _jsx(Line, {}), _jsxs(Container, { scrollY: true, style: {
                    maxHeight: "450px",
                }, children: [_jsx(Spacer, { y: "md" }), props.chainSelection && (_jsxs(Container, { px: "lg", children: [_jsx(Text, { size: "sm", children: "Select Network" }), _jsx(Spacer, { y: "xxs" }), _jsxs(SelectTokenBtn, { fullWidth: true, variant: "secondary", onClick: () => {
                                    setScreen("select-chain");
                                }, children: [_jsx(ChainIcon, { chainIconUrl: chainIconQuery.url, size: iconSize.lg, client: props.client }), chainNameQuery.name ? (_jsx(Text, { color: "primaryText", size: "sm", children: chainNameQuery.name })) : (_jsx(Skeleton, { height: fontSize.md })), _jsx(ChevronDownIcon, { width: iconSize.sm, height: iconSize.sm, style: {
                                            marginLeft: "auto",
                                        } })] }), _jsx(Spacer, { y: "xl" }), _jsx(Text, { size: "sm", children: "Select Token" })] })), _jsxs(Container, { px: "lg", children: [_jsx(Spacer, { y: "xs" }), _jsx(Input, { placeholder: locale.searchToken, variant: "outline", value: input, onChange: (e) => {
                                    setInput(e.target.value);
                                } })] }), _jsx(Spacer, { y: "md" }), (filteredList.length > 0 || !input) && (_jsxs(Container, { flex: "column", gap: "xs", px: "lg", scrollY: true, style: {
                            paddingTop: 0,
                            paddingBottom: spacing.lg,
                            // maxHeight: props.chainSelection ? "300px" : "400px",
                        }, children: [!input && (_jsx(SelectTokenButton, { onClick: () => {
                                    props.onTokenSelect(NATIVE_TOKEN);
                                }, chain: props.chain, token: NATIVE_TOKEN, client: props.client })), filteredList.map((token) => {
                                return (_jsx(SelectTokenButton, { onClick: () => props.onTokenSelect(token), token: token, chain: props.chain, client: props.client }, token.address));
                            })] })), filteredList.length === 0 && tokenQuery.isLoading && input && (_jsx(Container, { animate: "fadein", p: "lg", flex: "column", gap: "md", center: "both", style: {
                            minHeight: "200px",
                            paddingTop: 0,
                        }, color: "secondaryText", children: _jsx(Spinner, { size: "lg", color: "accentText" }) })), filteredList.length === 0 && !tokenQuery.isLoading && input && (_jsxs(Container, { animate: "fadein", p: "lg", flex: "column", gap: "md", center: "both", style: {
                            minHeight: "200px",
                            paddingTop: 0,
                        }, color: "secondaryText", children: [_jsx(CrossCircledIcon, { width: iconSize.lg, height: iconSize.lg }), locale.noTokensFound] })), _jsx(Spacer, { y: "md" })] })] }));
}
function SelectTokenButton(props) {
    const account = useActiveAccount();
    const tokenInfoQuery = useTokenInfo({
        chain: props.chain,
        tokenAddress: isNativeToken(props.token) ? undefined : props.token.address,
        client: props.client,
    });
    const tokenName = isNativeToken(props.token)
        ? tokenInfoQuery.data?.name
        : props.token.name;
    return (_jsxs(SelectTokenBtn, { fullWidth: true, variant: "secondary", onClick: props.onClick, children: [_jsx(TokenIcon, { token: props.token, chain: props.chain, size: "lg", client: props.client }), _jsxs(Container, { flex: "column", gap: "xxs", children: [tokenName ? (_jsx(Text, { size: "sm", color: "primaryText", children: tokenName })) : (_jsx(Skeleton, { height: fontSize.md, width: "150px" })), account && (_jsx(TokenBalance, { account: account, chain: props.chain, client: props.client, tokenAddress: isNativeToken(props.token) ? undefined : props.token.address }))] })] }));
}
function TokenBalance(props) {
    const tokenBalanceQuery = useQuery({
        queryKey: ["tokenBalance", props],
        queryFn: async () => {
            return getTokenBalance({
                account: props.account,
                chain: props.chain,
                client: props.client,
                tokenAddress: props.tokenAddress,
            });
        },
    });
    if (tokenBalanceQuery.data) {
        return _jsxs(Text, { size: "xs", children: [" ", formatTokenBalance(tokenBalanceQuery.data)] });
    }
    return _jsx(Skeleton, { height: fontSize.xs, width: "100px" });
}
const SelectTokenBtn = /* @__PURE__ */ styled(Button)(() => {
    const theme = useCustomTheme();
    return {
        background: theme.colors.tertiaryBg,
        justifyContent: "flex-start",
        gap: spacing.sm,
        padding: spacing.sm,
        "&:hover": {
            background: theme.colors.secondaryButtonBg,
            transform: "scale(1.01)",
        },
        transition: "background 200ms ease, transform 150ms ease",
    };
});
//# sourceMappingURL=TokenSelector.js.map