"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.FN_SELECTOR = void 0;
exports.isCastVoteWithReasonSupported = isCastVoteWithReasonSupported;
exports.encodeCastVoteWithReasonParams = encodeCastVoteWithReasonParams;
exports.encodeCastVoteWithReason = encodeCastVoteWithReason;
exports.castVoteWithReason = castVoteWithReason;
const prepare_contract_call_js_1 = require("../../../../../transaction/prepare-contract-call.js");
const encodeAbiParameters_js_1 = require("../../../../../utils/abi/encodeAbiParameters.js");
const once_js_1 = require("../../../../../utils/promise/once.js");
const detectExtension_js_1 = require("../../../../../utils/bytecode/detectExtension.js");
exports.FN_SELECTOR = "0x7b3c71d3";
const FN_INPUTS = [
    {
        type: "uint256",
        name: "proposalId",
    },
    {
        type: "uint8",
        name: "support",
    },
    {
        type: "string",
        name: "reason",
    },
];
const FN_OUTPUTS = [
    {
        type: "uint256",
    },
];
/**
 * Checks if the `castVoteWithReason` method is supported by the given contract.
 * @param availableSelectors An array of 4byte function selectors of the contract. You can get this in various ways, such as using "whatsabi" or if you have the ABI of the contract available you can use it to generate the selectors.
 * @returns A boolean indicating if the `castVoteWithReason` method is supported.
 * @extension VOTE
 * @example
 * ```ts
 * import { isCastVoteWithReasonSupported } from "thirdweb/extensions/vote";
 *
 * const supported = isCastVoteWithReasonSupported(["0x..."]);
 * ```
 */
function isCastVoteWithReasonSupported(availableSelectors) {
    return (0, detectExtension_js_1.detectMethod)({
        availableSelectors,
        method: [exports.FN_SELECTOR, FN_INPUTS, FN_OUTPUTS],
    });
}
/**
 * Encodes the parameters for the "castVoteWithReason" function.
 * @param options - The options for the castVoteWithReason function.
 * @returns The encoded ABI parameters.
 * @extension VOTE
 * @example
 * ```ts
 * import { encodeCastVoteWithReasonParams } from "thirdweb/extensions/vote";
 * const result = encodeCastVoteWithReasonParams({
 *  proposalId: ...,
 *  support: ...,
 *  reason: ...,
 * });
 * ```
 */
function encodeCastVoteWithReasonParams(options) {
    return (0, encodeAbiParameters_js_1.encodeAbiParameters)(FN_INPUTS, [
        options.proposalId,
        options.support,
        options.reason,
    ]);
}
/**
 * Encodes the "castVoteWithReason" function into a Hex string with its parameters.
 * @param options - The options for the castVoteWithReason function.
 * @returns The encoded hexadecimal string.
 * @extension VOTE
 * @example
 * ```ts
 * import { encodeCastVoteWithReason } from "thirdweb/extensions/vote";
 * const result = encodeCastVoteWithReason({
 *  proposalId: ...,
 *  support: ...,
 *  reason: ...,
 * });
 * ```
 */
function encodeCastVoteWithReason(options) {
    // we do a "manual" concat here to avoid the overhead of the "concatHex" function
    // we can do this because we know the specific formats of the values
    return (exports.FN_SELECTOR +
        encodeCastVoteWithReasonParams(options).slice(2));
}
/**
 * Prepares a transaction to call the "castVoteWithReason" function on the contract.
 * @param options - The options for the "castVoteWithReason" function.
 * @returns A prepared transaction object.
 * @extension VOTE
 * @example
 * ```ts
 * import { sendTransaction } from "thirdweb";
 * import { castVoteWithReason } from "thirdweb/extensions/vote";
 *
 * const transaction = castVoteWithReason({
 *  contract,
 *  proposalId: ...,
 *  support: ...,
 *  reason: ...,
 *  overrides: {
 *    ...
 *  }
 * });
 *
 * // Send the transaction
 * await sendTransaction({ transaction, account });
 * ```
 */
function castVoteWithReason(options) {
    const asyncOptions = (0, once_js_1.once)(async () => {
        return "asyncParams" in options ? await options.asyncParams() : options;
    });
    return (0, prepare_contract_call_js_1.prepareContractCall)({
        contract: options.contract,
        method: [exports.FN_SELECTOR, FN_INPUTS, FN_OUTPUTS],
        params: async () => {
            const resolvedOptions = await asyncOptions();
            return [
                resolvedOptions.proposalId,
                resolvedOptions.support,
                resolvedOptions.reason,
            ];
        },
        value: async () => (await asyncOptions()).overrides?.value,
        accessList: async () => (await asyncOptions()).overrides?.accessList,
        gas: async () => (await asyncOptions()).overrides?.gas,
        gasPrice: async () => (await asyncOptions()).overrides?.gasPrice,
        maxFeePerGas: async () => (await asyncOptions()).overrides?.maxFeePerGas,
        maxPriorityFeePerGas: async () => (await asyncOptions()).overrides?.maxPriorityFeePerGas,
        nonce: async () => (await asyncOptions()).overrides?.nonce,
        extraGas: async () => (await asyncOptions()).overrides?.extraGas,
        erc20Value: async () => (await asyncOptions()).overrides?.erc20Value,
    });
}
//# sourceMappingURL=castVoteWithReason.js.map