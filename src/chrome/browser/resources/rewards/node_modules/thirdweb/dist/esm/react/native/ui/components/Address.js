import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import Clipboard from "@react-native-clipboard/clipboard";
import { useState } from "react";
import { StyleSheet, TouchableOpacity } from "react-native";
import { spacing } from "../../design-system/index.js";
import { CHECK, COPY_ICON } from "../icons/svgs.js";
import { RNImage } from "./RNImage.js";
import { ThemedText } from "./text.js";
export const Address = ({ account, theme, addressOrENS }) => {
    const [copySuccess, setCopySuccess] = useState(false);
    return (_jsxs(TouchableOpacity, { style: styles.addressContainer, onPress: () => {
            // @ts-ignore - missing native types
            Clipboard.setString(account.address);
            setCopySuccess(true);
            setTimeout(() => {
                setCopySuccess(false);
            }, 2500);
        }, children: [_jsx(ThemedText, { theme: theme, type: "defaultSemiBold", children: addressOrENS }), _jsx(RNImage, { theme: theme, data: copySuccess ? CHECK : COPY_ICON, size: 15, color: copySuccess ? theme.colors.success : theme.colors.secondaryIconColor })] }));
};
const styles = StyleSheet.create({
    addressContainer: {
        flexDirection: "row",
        gap: spacing.sm,
        alignItems: "center",
    },
});
//# sourceMappingURL=Address.js.map