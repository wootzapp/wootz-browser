"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.uploadMetadata = uploadMetadata;
const ipfs_js_1 = require("../../../utils/ipfs.js");
const uploadMetadata_js_1 = require("../__generated__/BatchMetadataERC721/write/uploadMetadata.js");
/**
 * Uploads metadata for a batch of NFTs.
 * @param options - The options for the transaction.
 * @param options.contract - The contract to upload the metadata for.
 * @param options.metadatas - The metadata for the NFTs.
 * @returns The transaction to upload the metadata.
 * @modules BatchMetadataERC721
 * @example
 * ```ts
 * import { BatchMetadataERC721 } from "thirdweb/modules";
 *
 * const transaction = BatchMetadataERC721.uploadMetadata({
 *   contract,
 *   metadatas: [
 *     { name: "My NFT", description: "This is my NFT" },
 *   ],
 * });
 *
 * await sendTransaction({
 *   transaction,
 *   account,
 * });
 * ```
 */
function uploadMetadata(options) {
    return (0, uploadMetadata_js_1.uploadMetadata)({
        contract: options.contract,
        asyncParams: async () => {
            const batchOfUris = await (0, ipfs_js_1.uploadOrExtractURIs)(options.metadatas, options.contract.client);
            const baseURI = (0, ipfs_js_1.getBaseUriFromBatch)(batchOfUris);
            return {
                amount: BigInt(batchOfUris.length),
                baseURI,
            };
        },
    });
}
//# sourceMappingURL=uploadMetadata.js.map