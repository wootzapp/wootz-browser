/**
 * A hook that lets you set the active wallet.
 * @returns A function that lets you set the active wallet.
 * @example
 * ```jsx
 * import { useSetActiveWallet } from "thirdweb/react";
 *
 * const setActiveAccount = useSetActiveWallet();
 *
 * // later in your code
 * await setActiveAccount(account);
 * ```
 * @walletConnection
 */
export declare function useSetActiveWallet(): (activeWallet: import("../../../../exports/wallets.js").Wallet) => Promise<void>;
//# sourceMappingURL=useSetActiveWallet.d.ts.map