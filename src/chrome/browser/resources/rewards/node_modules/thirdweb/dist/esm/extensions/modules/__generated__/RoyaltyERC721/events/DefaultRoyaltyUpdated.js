import { prepareEvent } from "../../../../../event/prepare-event.js";
/**
 * Creates an event object for the DefaultRoyaltyUpdated event.
 * @param filters - Optional filters to apply to the event.
 * @returns The prepared event object.
 * @modules RoyaltyERC721
 * @example
 * ```ts
 * import { getContractEvents } from "thirdweb";
 * import { RoyaltyERC721 } from "thirdweb/modules";
 *
 * const events = await getContractEvents({
 * contract,
 * events: [
 *  RoyaltyERC721.defaultRoyaltyUpdatedEvent({
 *  recipient: ...,
 * })
 * ],
 * });
 * ```
 */
export function defaultRoyaltyUpdatedEvent(filters = {}) {
    return prepareEvent({
        signature: "event DefaultRoyaltyUpdated(address indexed recipient, uint256 bps)",
        filters,
    });
}
//# sourceMappingURL=DefaultRoyaltyUpdated.js.map