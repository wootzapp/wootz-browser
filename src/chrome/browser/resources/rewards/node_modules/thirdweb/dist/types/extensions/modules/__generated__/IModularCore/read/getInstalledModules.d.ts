import type { BaseTransactionOptions } from "../../../../../transaction/types.js";
import type { Hex } from "../../../../../utils/encoding/hex.js";
export declare const FN_SELECTOR: "0x3e429396";
/**
 * Checks if the `getInstalledModules` method is supported by the given contract.
 * @param availableSelectors An array of 4byte function selectors of the contract. You can get this in various ways, such as using "whatsabi" or if you have the ABI of the contract available you can use it to generate the selectors.
 * @returns A boolean indicating if the `getInstalledModules` method is supported.
 * @extension MODULES
 * @example
 * ```ts
 * import { isGetInstalledModulesSupported } from "thirdweb/extensions/modules";
 * const supported = isGetInstalledModulesSupported(["0x..."]);
 * ```
 */
export declare function isGetInstalledModulesSupported(availableSelectors: string[]): boolean;
/**
 * Decodes the result of the getInstalledModules function call.
 * @param result - The hexadecimal result to decode.
 * @returns The decoded result as per the FN_OUTPUTS definition.
 * @extension MODULES
 * @example
 * ```ts
 * import { decodeGetInstalledModulesResult } from "thirdweb/extensions/modules";
 * const result = decodeGetInstalledModulesResultResult("...");
 * ```
 */
export declare function decodeGetInstalledModulesResult(result: Hex): readonly {
    implementation: string;
    config: {
        registerInstallationCallback: boolean;
        requiredInterfaces: readonly `0x${string}`[];
        supportedInterfaces: readonly `0x${string}`[];
        callbackFunctions: readonly {
            selector: `0x${string}`;
        }[];
        fallbackFunctions: readonly {
            selector: `0x${string}`;
            permissionBits: bigint;
        }[];
    };
}[];
/**
 * Calls the "getInstalledModules" function on the contract.
 * @param options - The options for the getInstalledModules function.
 * @returns The parsed result of the function call.
 * @extension MODULES
 * @example
 * ```ts
 * import { getInstalledModules } from "thirdweb/extensions/modules";
 *
 * const result = await getInstalledModules({
 *  contract,
 * });
 *
 * ```
 */
export declare function getInstalledModules(options: BaseTransactionOptions): Promise<readonly {
    implementation: string;
    config: {
        registerInstallationCallback: boolean;
        requiredInterfaces: readonly `0x${string}`[];
        supportedInterfaces: readonly `0x${string}`[];
        callbackFunctions: readonly {
            selector: `0x${string}`;
        }[];
        fallbackFunctions: readonly {
            selector: `0x${string}`;
            permissionBits: bigint;
        }[];
    };
}[]>;
//# sourceMappingURL=getInstalledModules.d.ts.map