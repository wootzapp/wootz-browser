"use client";
import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { nativeLocalStorage } from "../../../utils/storage/nativeStorage.js";
import { createConnectionManager, } from "../../../wallets/manager/index.js";
import { ThirdwebProviderCore } from "../../core/providers/thirdweb-provider.js";
/**
 * The ThirdwebProvider is component is a provider component that sets up the React Query client.
 * @param props - The props for the ThirdwebProvider
 * @example
 * ```jsx
 * import { ThirdwebProvider } from "thirdweb/react";
 *
 * function Example() {
 *  return (
 *    <ThirdwebProvider>
 *      <App />
 *    </ThirdwebProvider>
 *   )
 * }
 * ```
 * @component
 */
export function ThirdwebProvider(props) {
    const connectionManager = useMemo(() => props.connectionManager || createConnectionManager(nativeLocalStorage), [props.connectionManager]);
    return (_jsx(ThirdwebProviderCore, { manager: connectionManager, children: props.children }));
}
//# sourceMappingURL=thirdweb-provider.js.map