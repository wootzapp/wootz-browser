<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="horizontal"
    android:background="@color/ai_suggestion_background"
    android:padding="8dp">

    <!-- Content container with text -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- Header text - always visible -->
        <org.chromium.ui.widget.TextViewWithLeading
            android:id="@+id/line_1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.TextLarge.Primary"
            app:leading="@dimen/text_size_large_leading"
            android:maxLines="1"
            android:ellipsize="end"
            android:textAlignment="viewStart"
            android:textColor="@color/ai_suggestion_text_color" />

        <!-- Subtext container with shimmer and typing animation -->
        <FrameLayout
            android:id="@+id/subtext_container"
            android:layout_width="match_parent"
            android:layout_height="24dp">

            <!-- Shimmer effect for subtext -->
            <View
                android:id="@+id/subtext_shimmer"
                android:layout_width="match_parent"
                android:layout_height="24dp"
                android:layout_gravity="center_vertical"
                android:background="@drawable/ai_suggestion_loading_shimmer"
                android:alpha="0.3" />

            <!-- Actual subtext with typing animation -->
            <TextView
                android:id="@+id/line_2"
                android:layout_width="match_parent"
                android:layout_height="24dp"
                android:layout_gravity="center_vertical"
                android:textAppearance="@style/TextAppearance.TextMedium.Secondary"
                android:maxLines="1"
                android:visibility="gone"
                android:ellipsize="end"
                android:textAlignment="viewStart"
                android:textColor="@color/ai_suggestion_subtext_color"
                android:gravity="center_vertical" />

        </FrameLayout>

    </LinearLayout>

    <!-- Right arrow icon -->
    <ImageView
        android:id="@+id/arrow_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="8dp"
        android:src="@drawable/ic_arrow_forward"
        android:visibility="visible" />

</LinearLayout>
