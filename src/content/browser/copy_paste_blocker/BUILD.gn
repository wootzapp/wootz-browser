source_set("copy_paste_blocker") {
  sources = [
    "copy_paste_blocked_snackbar_bridge.cc",
    "copy_paste_blocked_snackbar_bridge.h",
  ]

  deps = [
    "//base",
    "//url",
  ]

  if (is_android) {
    deps += [
      "//chrome/android:chrome_jni_headers",
      "//ui/android",
    ]
  }
}